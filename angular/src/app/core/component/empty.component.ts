import { Component, Injectable, inject, signal } from "@angular/core";

@Component({
    standalone: true,
    selector: 'nav-link',
    template: ``,
    styles: ``,
})
export default class Empty {
}